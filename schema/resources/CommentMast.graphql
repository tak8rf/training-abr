type CommentMast {
		postID: ID!
		commentUserID: ID!
		commentID: ID!
		comment: String
		createdAt: AWSTimestamp!
}

input CommentMastInput {
		postID: ID!
		commentUserID: ID!
		commentID: ID!
		comment: String
		createdAt: AWSTimestamp!
}